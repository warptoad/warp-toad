version: '3.8'

services:
  bridge-sync:
    build:
      context: ..
      dockerfile: bridgeSync/Dockerfile
    container_name: bridge-sync
    ports:
      - "6969:6969"
    environment:
      - NODE_ENV=production
      - PORT=6969
    env_file:
      - .env
    volumes:
      - ./logs:/app/bridgeSync/logs
      - ./db:/app/bridgeSync/db
    restart: unless-stopped
    networks:
      - bridge-network

networks:
  bridge-network:
    driver: bridge
